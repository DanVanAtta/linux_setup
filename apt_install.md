```
sudo apt-get update

sudo apt-get install -y npm vim gimp htop traceroute clamav shellcheck fail2ban \
  nmap iftop dos2unix git meld google-chrome-stable ntp ntpdate \
  psensor unrar zip unzip p7zip-rar \
  sharutils rar file-roller parallel expect ruby ruby-dev libxml2-utils

sudo npm install -g diff-so-fancy
sudo gem install travis -v 1.8.2 --no-rdoc --no-ri
sudo sensors-detect --auto
sudo apt-get -y upgrade
sudo apt autoremove -y
```
